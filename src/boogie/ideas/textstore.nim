# d. Full-Text Search Store
# Description: Store documents and provide fast text search (inverted index).
# How: Table with text columns, plus an inverted index mapping terms to document IDs. Use a module to build and query the index.
# Example: Elasticsearch-lite.